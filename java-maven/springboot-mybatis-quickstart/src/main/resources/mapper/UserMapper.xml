<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.doub1e.mapper.UserMapper">
    <!--查询全部用户数据-->
    <select id="list" resultType="com.doub1e.entity.User">
        select id,username,password,name,age from user
    </select>
</mapper>